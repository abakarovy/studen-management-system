# Система учета студентов группы

Минимально рабочий прототип (MVP) веб-приложения для учета студентов группы с раздельной серверной (бэкенд) и клиентской (фронтенд) частями.

## 📁 Структура проекта

```
/
├── backend/          # Серверная часть (Node.js + Express)
├── frontend/         # Клиентская часть (Vue 3)
├── .gitignore
├── docker-compose.yml
└── README.md
```

## 🚀 Быстрый старт

### Предварительные требования

- Node.js (версия 18 или выше)
- npm или yarn

### Установка и запуск

#### 1. Установка зависимостей

```bash
# Установка зависимостей для backend
cd backend
npm install

# Установка зависимостей для frontend
cd ../frontend
npm install
```

#### 2. Настройка переменных окружения

**Backend:**

Скопируйте `.env.example` в `.env` в папке `backend/`:

```bash
cd backend
copy .env.example .env  # Windows
# или
cp .env.example .env     # Linux/Mac
```

Отредактируйте `.env` файл при необходимости:

```env
PORT=3000
NODE_ENV=development
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
DB_PATH=./database.sqlite
```

**Frontend:**

Скопируйте `.env.example` в `.env` в папке `frontend/`:

```bash
cd frontend
copy .env.example .env  # Windows
# или
cp .env.example .env     # Linux/Mac
```

Отредактируйте `.env` файл при необходимости:

```env
VITE_API_URL=http://localhost:3000/api
```

#### 3. Инициализация базы данных и заполнение тестовыми данными

```bash
cd backend
npm run seed
```

Это создаст базу данных SQLite и заполнит её тестовыми данными:
- 1 куратор
- 2 преподавателя
- 2 группы
- 10 студентов
- 4 дисциплины
- Оценки для студентов

#### 4. Запуск приложения

**В первом терминале (Backend):**

```bash
cd backend
npm run dev
```

Backend будет доступен по адресу: http://localhost:3000

**Во втором терминале (Frontend):**

```bash
cd frontend
npm run dev
```

Frontend будет доступен по адресу: http://localhost:5173

## 👥 Тестовые учетные данные

После выполнения `npm run seed` в папке backend, вы можете войти с следующими учетными данными:

### Куратор (полный доступ)
- **Email:** curator@example.com
- **Пароль:** curator123

### Преподаватель
- **Email:** teacher1@example.com
- **Пароль:** teacher123
- **Email:** teacher2@example.com
- **Пароль:** teacher123

### Студенты
- **Email:** student1@example.com - student10@example.com
- **Пароль:** student123 (для всех студентов)

## 🎯 Функциональность по ролям

### Студент
- Просмотр своего профиля (ФИО, группа, контакты)
- Просмотр своей успеваемости (таблица: дисциплина, дата, оценка, тип работы)
- Просмотр статистики успеваемости на главной странице

### Преподаватель
- Просмотр списка своих групп и студентов в них
- Просмотр своих дисциплин
- Внесение и редактирование оценок для студентов своей группы по дисциплинам
- Просмотр оценок по своим дисциплинам

### Куратор (администратор)
- Всё, что может преподаватель
- Управление пользователями (добавление, удаление, редактирование, назначение ролей)
- Управление группами (создание, редактирование, удаление)
- Управление дисциплинами (создание, редактирование, удаление, привязка преподавателей)
- Полный доступ ко всем данным системы

## 🛠 Технологический стек

### Backend
- **Node.js** с **Express** - веб-сервер
- **SQLite** с **better-sqlite3** - база данных
- **JWT** (jsonwebtoken) - аутентификация
- **bcryptjs** - хеширование паролей

### Frontend
- **Vue 3** (Composition API) - фронтенд фреймворк
- **Vue Router** - маршрутизация
- **Pinia** - управление состоянием
- **Axios** - HTTP-клиент
- **Tailwind CSS** - стилизация

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация (только для куратора)
- `GET /api/auth/me` - Получить информацию о текущем пользователе

### Пользователи
- `GET /api/users` - Список пользователей (куратор)
- `GET /api/users/:id` - Информация о пользователе
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя (куратор)

### Оценки
- `GET /api/grades` - Список оценок (с фильтрами)
- `POST /api/grades` - Добавить оценку (преподаватель/куратор)
- `PUT /api/grades/:id` - Обновить оценку (преподаватель/куратор)
- `DELETE /api/grades/:id` - Удалить оценку (преподаватель/куратор)

### Группы
- `GET /api/groups` - Список групп
- `GET /api/groups/:id` - Информация о группе и студентах
- `POST /api/groups` - Создать группу (куратор)
- `PUT /api/groups/:id` - Обновить группу (куратор)
- `DELETE /api/groups/:id` - Удалить группу (куратор)

### Дисциплины
- `GET /api/subjects` - Список дисциплин
- `GET /api/subjects/:id` - Информация о дисциплине
- `POST /api/subjects` - Создать дисциплину (куратор)
- `PUT /api/subjects/:id` - Обновить дисциплину (куратор)
- `DELETE /api/subjects/:id` - Удалить дисциплину (куратор)

## 🐳 Запуск через Docker (опционально)

```bash
docker-compose up
```

Это запустит оба сервиса (backend и frontend) в контейнерах Docker.

## 📝 Скрипты

### Backend
- `npm start` - Запуск в production режиме
- `npm run dev` - Запуск в development режиме с hot-reload
- `npm run seed` - Заполнение базы данных тестовыми данными

### Frontend
- `npm run dev` - Запуск development сервера
- `npm run build` - Сборка для production
- `npm run preview` - Предпросмотр production сборки

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены используются для аутентификации
- Проверка ролей на уровне API
- CORS настроен для безопасности

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Проект создан как MVP. Для production использования рекомендуется:
- Добавить валидацию данных на клиенте и сервере
- Улучшить обработку ошибок
- Добавить логирование
- Настроить CI/CD
- Добавить тесты
- Улучшить UI/UX

